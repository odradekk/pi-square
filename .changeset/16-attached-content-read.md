---
"@odradekk/pi-square": minor
---

Deliver bounded attached content through Read with tree rails.

Changes the default display policy to `preview` with nine collapsed body rows. Introduces head/tail content preservation with source-line-accurate omission counts, tree-rail body connectors (`│`/`└─`), `…` truncation for no-wrap mode, and explicit lifecycle routing for the Read tool with partial-result awareness. Public Adapter v1, model-facing schemas, image reads, and hidden-policy error visibility remain unchanged.
