---
"@odradekk/pi-square": minor
---

Give the bundled Oracle role observational shell access and the `search`, `fetch`, `libs`, and `docs` tools so it can reproduce defects and confirm version-specific third-party behavior. Oracle stays non-mutating through its policy, keeps no write, edit, GitHub, or skill access, and its capabilities now contain Crawler's; see ADR-0006.
