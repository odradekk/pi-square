---
"@odradekk/pi-square": major
---
Rename `subagent_delegate` to `delegate` and `subagent_resume` to `resume`. The old names are retired with no aliases. Update subagent definitions that reference the old names.
