---
"@odradekk/pi-square": patch
---

Context Memory: render each current Memory block as one ordered provider-bound text content block for every model and provider (one uniform projection, no cache markers, no provider branch, fail-safe to the ordinary summary), and re-pin the provider-cache experiment to measure a real cross-compaction append — the multi-block projection against the single-summary-block baseline under a content-divergence control.
