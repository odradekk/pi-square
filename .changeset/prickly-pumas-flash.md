---
"@odradekk/pi-square": patch
---

Context Memory: render each current Memory block as one ordered provider-bound text content block for every model and provider (one uniform projection, no cache markers, no provider branch, fail-safe to the ordinary summary), and measure its cache behavior through real Pi request sequences that execute the production tool and compaction lifecycle.
